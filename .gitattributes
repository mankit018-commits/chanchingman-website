# Normalize line endings so the served bytes match the CSP hash for the
# inline JSON-LD block. All text is stored and served with LF.
* text=auto eol=lf

# Treat images as binary (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
